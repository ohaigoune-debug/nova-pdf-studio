# خطة التنفيذ — IMPLEMENTATION_PLAN

المبدأ: **Vertical Slice** حقيقي يعمل من طرف إلى طرف قبل التوسّع. بعد كل مرحلة: `npm run typecheck && npm run lint && npm test` ثم تشغيل التطبيق.

## المرحلة 1 — الأساس ✅

- [x] Next.js 15 + TS strict + Tailwind + مكوّنات UI بأسلوب shadcn + RTL + Dark Mode
- [x] Drizzle schema كامل للجداول الأساسية + Migrations + PGlite/Postgres client
- [x] مصادقة: تسجيل، دخول، خروج، جلسات في DB، كوكي httpOnly، إدارة الأجهزة
- [x] الأدوار + حرّاس الخادم (`requireActor`, `requireRole`)
- [x] i18n (ar أساسي، بنية لإضافة fr/en)
- [x] الأصداف: Public / Student / Teacher / Admin (Sidebar + TopBar + Navigation)
- [x] `AppError` + رسائل عربية، سجل تدقيق، إشعارات داخل التطبيق

## المرحلة 2 — الأفواج والتسجيل ✅

- [x] الولايات (58) + المدارس + السنوات + المستويات + الشعب (Seed)
- [x] Admin: إنشاء/تعطيل أستاذ (ينشئ workspace)
- [x] Teacher: CRUD الأفواج + Dashboard الفوج
- [x] أكواد التسجيل: كود واحد/دفعة، تعطيل، إلغاء دفعة، تصدير CSV، طباعة
- [x] الطالب: إنشاء حساب → إدخال كود → انضمام تلقائي للفوج
- [x] ملف الطالب (Teacher view) + سجل الحالات

## المرحلة 3 — الحضور الذكي ✅

- [x] الحصص: بدء/إنهاء/إلغاء، حصة مفتوحة واحدة لكل فوج
- [x] QR ديناميكي موقّع (HMAC، 60 ثانية، nonce) في حساب الطالب
- [x] Scanner Mode (قارئ USB/Bluetooth كلوحة مفاتيح) + ردود فورية + صوت
- [x] حالات الحضور PRESENT/LATE/ABSENT/EXCUSED/UNEXCUSED + late_after_minutes
- [x] إنهاء الحصة: غياب تلقائي UNEXCUSED + إعادة حساب + قاعدة 4 غيابات ⇒ تعليق
- [x] تبرير الغياب (سبب/ملاحظات) + إعادة تفعيل الطالب يدوياً
- [x] الحضور في ملف الطالب + Timeline + إشعارات الغياب/التعليق
- [x] اختبارات: الكود، الحضور، QR منتهٍ، فوج خاطئ، تكرار، تأخر، إغلاق، غياب تلقائي، تعليق، تبرير، عزل الأساتذة، عزل الطلاب

## المرحلة 4 — المحتوى والواجبات ✅

- [x] CMS للأستاذ: أنواع المحتوى + Visibility (عام/طلاب/أفواج/طلاب محددون) + Targets + إشعار المستهدفين + بحث شامل في المساحة
- [x] Storage: رفع ملفات (Local adapter، S3/Supabase Storage لاحقاً بنفس الواجهة) + روابط موقّعة تنتهي بعد 10 دقائق + تحقق MIME/الحجم
- [x] الواجبات + الإسناد (فوج/أفواج/طلاب محددون) + إشعار الطلاب + لوحة الإجابات ونسبة الإنجاز
- [x] إجابة الطالب **نصاً مكتوباً كرسالة** (حفظ مسودة تلقائي، إرسال نهائي لا يُعدَّل، تعليم التأخر عن الأجل)؛ لا تصوير ولا OCR
- [x] سلسلة رسائل لكل إجابة: ANSWER ← FEEDBACK ← REPLY، لا تعديل ولا حذف
- [x] اعتماد تصحيح الأستاذ (نقطة + أحسن في + يحتاج تحسين + ملاحظة) → يصل الطالب كرسالة تصحيح وعلامة معتمدة
- [x] الصفحات العامة وصفحات الطالب تقرأ من `content` حسب الرؤية
- [x] اختبارات: الإسناد والعزل، المسودة والقفل بعد الإرسال، الرسائل، التصحيح والعلامة، رفع الملفات والروابط الموقّعة، رؤية المحتوى، البحث

## المرحلة 5 — الاختبارات والعلامات ✅

- [x] Quiz Engine بسبعة أنواع (MCQ متعدد الصحيح، صح/خطأ، إجابة قصيرة بتطبيع عربي متسامح، مقالية، ملء فراغ بنقاط جزئية، مطابقة بنقاط جزئية، سؤال بصورة) + محرّر أسئلة + مؤقّت + عدد محاولات + أجل
- [x] تصحيح آلي فوري للموضوعي؛ المقالي ينتظر الأستاذ ثم تُعتمد النتيجة وتظهر مفاتيح الإجابة للطالب بعد الاعتماد فقط
- [x] قفل الأسئلة بعد أول محاولة (حماية الإجابات المخزّنة)
- [x] Rubrics قابلة للتعديل (بنود + نقاط + مهارة لكل بند) + قوالب عامة من المشرف + تصحيح الواجبات بنداً بنداً
- [x] Grades موحّدة للواجبات والاختبارات، مرئية للطالب بعد الاعتماد
- [x] اختبارات: التصحيح الآلي لكل نوع، العزل، حدود المحاولات، قفل الأسئلة، الشبكة والتحقق من النقاط

## المرحلة 6 — التصحيح بالذكاء الاصطناعي

- [x] **Skills Engine** (نُفِّذ مبكراً): `student_skills` بمتوسط متحرك وثقة تتراكم + `student_skill_history`؛ يُغذَّى من الاختبارات (لكل مهارة سؤال) ومن تصحيح الأستاذ (مهارة الواجب وبنود الشبكة)؛ خريطة مهارات للطالب وللأستاذ، والمهارات الضعيفة المشتركة في الفوج، ومحتوى مقترح لتقوية المهارات الضعيفة (تعلّم تكيفي مبسّط)
- [x] `AIProvider` (`evaluateEssay`, `generateTeacherInsights`) + مزوّد تجريبي حتمي + مزوّد Anthropic حقيقي يُفعَّل من البيئة (`AI_PROVIDER`, `AI_API_KEY`, `AI_MODEL`)؛ لا مفتاح في المتصفح
- [x] طابور `jobs` حقيقي (التقاط بقفل، إعادة محاولة بتراجع أسّي، عامل داخلي + Cron `POST /api/v1/jobs/run` + `npm run jobs:worker`)
- [x] التصحيح على النص المكتوب: إجابة نصية → Job → اقتراح (نقطة، توزيع على بنود الشبكة، قوة/ضعف/أخطاء/مهارات، ملاحظة) → لوحة الأستاذ (تعبئة النموذج/تعديل/رفض) → تظهر للطالب كرسالة تصحيح بعد الاعتماد فقط. لا OCR (قرار مالك المنصة)
- [x] Teacher Review في `teacher_reviews` (APPROVED/EDITED/REJECTED) + قاعدة "AI لا يكتب فوق علامة الأستاذ" مختبَرة + إخفاء كامل عن الطالب (الحالة والنصوص) + تقييم تلقائي اختياري عند الإرسال يفعّله المشرف
- [ ] `analyzeStudent` و`generateExercises` (توليد تمارين علاجية) — لاحقاً فوق نفس الواجهة

## المرحلة 7 — التحليلات والتوصيات

- [x] تحليلات مبنية على البيانات (بلا نموذج لغوي): ضعف مشترك في مهارة داخل الفوج، من لم يرسل آخر واجبين، تراجع/تحسّن الحضور، المتأخرون، المعرّضون للتعليق؛ وخريطة حرارية للحضور في لوحة الفوج
- [x] صياغة التحليلات وتوصيات الحصة القادمة عبر `AIProvider.generateTeacherInsights` كمهمة خلفية (الحقائق من قاعدة البيانات، النموذج يصوغ فقط)
- [ ] ربط الغياب بالمستوى (توصية لا حكم)
- [ ] Adaptive Learning (Weak Skill ⇒ درس + ملخص + 3 تمارين + اختبار قصير)
- [ ] تقارير PDF/CSV (طالب/فوج/حضور/علامات/تقدّم) كـ Jobs
- [ ] Push Notifications (PWA)

## المرحلة 8 — التحصين والإنتاج

- [ ] Rate limiting على الدخول/الأكواد/المسح، CSRF (Origin check على Actions)، CSP
- [ ] RLS على Postgres/Supabase، Signed URLs، تنظيف nonces/جلسات
- [ ] PWA (manifest + service worker + offline shell)
- [ ] Docker + متغيّرات بيئة + دليل النشر

## ما بعد الإطلاق (لا يُبنى الآن)

اشتراكات/دفع، ولي أمر، دردشة، حصص مباشرة، شهادات، Marketplace، تطبيق جوال (يستهلك `/api/v1`).
